import React from 'react';
import { Button } from 'react-materialize';

const AddTask = () => (
  <Button floating large className="add-task-btn" waves="light" icon="add" />
);
export default AddTask;
